[project]
name = "Pac-man"
version = "0.0.1"
description = "the man that pacs"
requires-python = "~=3.10.0"
dependencies = [
	"pygame",
    "pyinstaller"
]

[dependency-groups]
dev = [
    "mypy>=2.1.0",
]
